# Matrix Server Configuration
# Copy this file to .env and update with your values

# Domain Configuration
MATRIX_DOMAIN=matrix.yourdomain.com
ELEMENT_DOMAIN=element.yourdomain.com

# Database Configuration
POSTGRES_DB=synapse
POSTGRES_USER=synapse
POSTGRES_PASSWORD=your-secure-password-here

# Matrix Secrets (generate random 32-byte hex strings)
# Use: openssl rand -hex 32
REGISTRATION_SHARED_SECRET=your-registration-secret-here
TURN_SHARED_SECRET=your-turn-secret-here

# TURN Server Configuration (for voice/video calls)
# Your server's external IP address (for NAT traversal)
# Leave empty to auto-detect via ipinfo.io or icanhazip.com
# EXTERNAL_IP=your.server.public.ip

# Network Configuration
NETWORK_NAME=matrix-network
NETWORK_EXTERNAL=true

# Portainer Configuration (optional)
# PORTAINER_REDEPLOY_HOOK=https://your-portainer-instance.com/api/webhooks/your-webhook-id